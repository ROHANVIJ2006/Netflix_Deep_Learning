# Virtual environments
.venv/
new_env/
env/
venv/
ENV/

# Python cache
__pycache__/
*.py[cod]
*$py.class

# IDE settings
.idea/
.vscode/

# Project specific
scratch/
model/*.pkl
model/*.csv
# Note: I am NOT ignoring model files because Vercel needs them and the user likely wants them in the repo for deployment.
# However, if they are too big, I should ignore them. 
# They are small (~10KB), so it's fine.

# Logs
*.log
